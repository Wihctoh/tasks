// Напишите программу, которая находит сумму всех чисел от 1 до 100 и выводит
// результат в консоль.

let count: number = 0;

for (let i: number = 0; i < 100; i++) {
  count += i;
}

console.log(count);
