// Напишите программу, которая считывает длины двух катетов в прямоугольном
// треугольнике и выводит его площадь. Используйте формулу:
// S = 1/2 ∙a ∙ b

const a = +prompt('введите первую сторону прямоугольника');
const b = +prompt('введите вторую сторону прямоугольника');

console.log(1 / 2 * a * b);